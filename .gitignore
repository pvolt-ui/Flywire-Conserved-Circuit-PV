# Python
.venv/
__pycache__/
*.pyc

# Large provided connectome edge lists (the five challenge CSVs) — not redistributed.
# Download from the FlyWire Codex; see README. Keep them at the repo root locally.
*_edge_list.csv
*.csv:Zone.Identifier
*:Zone.Identifier

# Cached cleaned graphs (regenerated by src/io_utils.py)
data/clean/

# Codex metadata tables (fetched by src/annotate.py from public GCS; see README)
data/meta/
